number = int(input())
sum = 0
for num in range(1, number + 1):
    for digit in str(num):
        sum += int(digit)
    if sum == 11 or sum == 5 or sum == 7:
        print(f"{num} -> True")
    else:
        print(f"{num} -> False")
    sum = 0
